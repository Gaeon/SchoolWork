#version 400 core
//struct Material {
//    vec3 ambient;
//    vec3 diffuse;
//    vec3 specular;
//    float shininess;
//}
//uniform Material material;

uniform vec4 color;
//out vec3 color;
in vec4 v_Color;
in vec4 v2_Color;
void main()
{
    //gl_FragColor=vec4(0,1,0,1);
    //color = vec3(0, 0, 1);
    //gl_FragColor=vec4(v_Color,1);
    gl_FragColor=v_Color;
    //gl_FragColor=v2_color;
}
